use std::str::FromStr;
use crate::tupple::{Tupple, Data};

grammar;

pub Expr : Tupple = { 
    "(" <l:Content> "," <r:Content> ")" => Tupple(l, r)
};

Content : Data = {
    Num => Data::Number(<>)
}

Num: i32 = {
    r"[0-9]+" => i32::from_str(<>).unwrap()
};

