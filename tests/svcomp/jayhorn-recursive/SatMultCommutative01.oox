class Main {
  // n = 1, m = 2
  static int mult(int n, int m) {
    if (m == 0) {
      return 0;
    };
    int mult := Main.mult(n, m - 1);
    return n + mult;
  }

  static void main(int m, int n) {
    if (m < 0 || m > 46340) {
      return;
    };
    if (n < 0 || n > 46340) {
      return;
    };
    int res1 := Main.mult(m, n);
    int res2 := Main.mult(n, m);
    if (res1 != res2 && m > 0 && n > 0) {
      assert false;
    } else {
      return;
    }
  }
}
