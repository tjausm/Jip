class Main{
    static void main(){
        Main.mod();
    }
    static int mod(int a, int b){
        assume a >= 0 && b >= 0;
        int res := a;

        while (res > b) { // bug: > should be >=
            res := res - b;
        }
        assert res == a % b;
        return res;
    }

}

