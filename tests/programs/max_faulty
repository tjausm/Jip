class Main {
    static void main(){
        int x; 
        int y; 
        int z; 
        if (y >= x) 
            z := x; 
        else 
            z := y; 
        assert z >= x && z >= y;
    }
}

