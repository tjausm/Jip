#[test]
fn {name}() {{

    let predicate = contains("Program is correct").not();
    let mut cmd = Command::cargo_bin("jip").unwrap();
    let assert = cmd
        .arg("verify-file")
        .arg(r#"{path}"#)
        .assert();
    assert
        .success()
        .stdout(predicate);
}}