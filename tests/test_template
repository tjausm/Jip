#[allow(non_snake_case)]
#[test]
fn {name}() {{
    let mut cmd = Command::cargo_bin("jip").unwrap();
    let assert = cmd
        .arg("file")
        .arg(r#"{path}"#)
        .arg("verify")
        .assert();
    assert
        .code(0);
}}