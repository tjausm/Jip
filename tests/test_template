#[test]
fn {name}() {{
    let mut cmd = Command::cargo_bin("jip").unwrap();
    let assert = cmd
        .arg("verify-file")
        .arg(r#"{path}"#)
        .assert();
    assert
        .code(0);
}}